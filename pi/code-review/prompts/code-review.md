---
description: Run an independent multi-agent review of the current branch
argument-hint: "[additional instructions]"
---

Load the `code-review` skill with `skill({ name: "code-review" })`, then execute
an independent code review of the current branch using that skill.

Additional instructions:

${ARGUMENTS:-None.}
