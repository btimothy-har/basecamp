---
description: Run an independent multi-agent review of the current branch
argument-hint: "[additional instructions]"
---

Load the `code-review` skill with `skill({ name: "code-review" })`, then apply
that guidance to run an independent code review of the current branch.

Additional instructions:

${ARGUMENTS:-None.}
