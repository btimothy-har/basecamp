## Logseq Graph

The user maintains a Logseq knowledge graph at `{{LOGSEQ_GRAPH}}`. It contains project notes, decisions, meeting notes, and planning context. Use it as a **read-only reference** when you need background on a project or decision.

- Journal entries: `journals/YYYY_MM_DD.md`
- Topic pages: `pages/<Page Name>.md`
- Search with `grep -r` for keywords when looking for prior decisions or context
