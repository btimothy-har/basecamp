# Managing Projects

Project configuration is managed through the projects menu:

```bash
basecamp projects
```

Use it to list, add, edit, or remove configured projects. The full schema lives in the [projects.json reference](../configuration/projects.md).
