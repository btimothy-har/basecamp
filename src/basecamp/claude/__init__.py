"""The basecamp Claude foundation.

A self-contained parallel to ``basecamp.core`` for the Claude Code plugin: its
own config (``~/.claude/basecamp.json``), repo identity, and shared-Logseq page
resolution — with no dependency on the ``~/.pi`` config, the Pi extension, or the
basecamp daemon.
"""
